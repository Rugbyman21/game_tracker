<div class="jumbotron">
  <div class="container">
    <h1>Games</h1>
    <p><h3>Here are all the Games!</h3></p>
  </div>
</div>

<div class="row">
  <% @games.each do |game| %>
    <div class="col-md-3">
      <h3><%= link_to "#{game.title}", genre_games_path(game.id) %></h3>
    </div>
  <% end %>
</div>
<%= link_to "Add New Game", new_genre_game_path %>
